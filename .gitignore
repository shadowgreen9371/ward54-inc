# Claude Code local config (preview server settings, etc.)
.claude/

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
*~

# Logs / temp
*.log
tmp/

# Node / pnpm
node_modules/
.pnpm-store/
*.tsbuildinfo

# Next.js
.next/
out/
next-env.d.ts

# Env files — never commit
.env
.env.local
.env.*.local

# Supabase
supabase/.branches/
supabase/.temp/
supabase/.env

# Electoral Roll source PDFs — kept local only, JSON output is committed
data/pdfs/Part_*.pdf

# OCR pipeline working files — kept local only
.tools/
data/ocr-text/
data/ocr-extract.log
data/ocr-extract.pid
eng.traineddata
*.bak
*.bak2
*.bak3

# iOS — managed by CocoaPods + Capacitor (don't commit)
mobile/ios/App/Pods/
mobile/ios/App/Podfile.lock
mobile/ios/DerivedData/
mobile/ios/.DS_Store
