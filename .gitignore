# Claude Code local config (preview server settings, etc.)
.claude/

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
*~

# Logs / temp
*.log
tmp/

# Node / pnpm
node_modules/
.pnpm-store/
*.tsbuildinfo

# Next.js
.next/
out/
next-env.d.ts

# Env files — never commit
.env
.env.local
.env.*.local

# Supabase
supabase/.branches/
supabase/.temp/
supabase/.env

# Electoral Roll source PDFs — kept local only, JSON output is committed
data/pdfs/Part_*.pdf

# OCR pipeline working files — kept local only
.tools/
data/ocr-text/
data/ocr-extract.log
data/ocr-extract.pid
eng.traineddata
*.bak
*.bak2
*.bak3

# iOS — managed by CocoaPods + Capacitor (don't commit)
mobile/ios/App/Pods/
mobile/ios/App/Podfile.lock
mobile/ios/DerivedData/
mobile/ios/.DS_Store

# Hostinger deploy credentials (never commit)
grihine-soap/.deploy.env

# Nested standalone repo (own git history, own GitHub repo)
grihine-soap/

# Android/iOS build artifacts + signing keystore — NEVER commit (secrets)
mobile/release/
mobile/ios/App/App.xcworkspace/xcshareddata/

# Separate project living in this folder (has its own repo)
apps/phone-workstation/
data/phone_db_schema.sql

# Android local build config (contains signing passwords) — never commit
mobile/android/gradle.properties
