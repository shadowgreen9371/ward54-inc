# Capacitor / Android build artifacts — never commit
node_modules/
android/.gradle/
android/.idea/
android/local.properties
android/app/build/
android/build/
android/captures/
android/.cxx/
android/app/release/
*.iml

# Local cache that's regenerated by `npx cap sync`
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json

# ⚠ RELEASE SIGNING — NEVER COMMIT THESE
android/gradle.properties
release/ward54inc-release.jks
release/KEYSTORE-CREDENTIALS-DO-NOT-LOSE.txt
release/*.keystore
release/*.jks
